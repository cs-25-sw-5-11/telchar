# Using FMM STMatch

## Guide:
- Build and install FMM
- Load input files into data/cleaned_data (STMatch requires semicolon separated csv files)
- Run dl.py to download OSM data
- Run main.py to generate output data for each input file
- Use avg.py to generate an average file for the days
